<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>EGOR DRUMS</title>
    <style >
      body {
        height: 100vh;
        margin: 0;
        display: flex;
        flex-direction: column;
        color: white;
        padding-bottom: 20px;
        font-family:'Gill Sans', 'Gill Sans MT', Calibri, 'Trebuchet MS', sans-serif
      }
      .content {
        flex: 1;
        justify-content: center;
        align-items: center;
        text-align: center;
        color: white;
        text-decoration: none;
      }

      nav {
        font-size: 20px;
        background-color: rgba(0, 0, 0, 0.8);
        padding: 10px 0;
        width: 100%;
        text-align: right;
        position: fixed;
        top: 0;
        right: 10px;
        z-index: 1000;
      }

      nav a {
        color: white;
        text-decoration: none;
        padding: 0 10px;
      }

      nav a:hover {
        background-color: #030303;
      }

      nav form {
        display: inline;
      }

      footer {
        text-align: center;
        width: 100%;
        position: fixed;
        bottom: 0;
        background-color: rgba(0, 0, 0, 0.8);
      }

      h1 {
        font-size: 5rem;
        text-align: center;
      }
      h3 {
        font-size: 1.5rem;
        text-align: center;
      }

      form {
        margin-top: 5px;
      }

      button {
        padding: 10px 20px;
        font-size: 1rem;
      }

      .error,
      .info {
        margin-top: 10px;
        padding: 10px;
      }

      .error {
        background-color: #ffcccc;
      }

      .info {
        background-color: #ccffcc;
      }

      #background-video {
      position: fixed;
      top: 50%;
      left: 50%;
      min-width: 100%;
      min-height: 100%;
      width: auto;
      height: auto;
      transform: translate(-50%, -50%);
      z-index: -1;
    }
    </style>
  </head>
  <body>
    <video autoplay muted loop id="background-video">
      <source src="/EgorD.mp4" type="video/mp4" />
    </video>